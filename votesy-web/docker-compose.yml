version: '3.4'

services:
  votesyweb:
    image: votesyweb
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5002:5002
    environment:
      - apiUrl=[apiurl]
      - resultsUrl=[resultsUrl]
